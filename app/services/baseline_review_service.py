def run_review(prompt, client, model):
    ...
    # response = client.chat.completions.create(
    #     model=model,
    #     messages=[{"role":"user","content":prompt}],
    #     max_tokens=4000
    # )
    # return json.loads(response.choices[0].message.content)
